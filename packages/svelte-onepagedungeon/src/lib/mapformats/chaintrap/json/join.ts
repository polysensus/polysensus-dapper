/** A JoinSides identifies the location side a join enters, used in association with {Join} */
export type JSONJoinSides = [number, number];

/**
 * A Join is a pair of location id's, used in association with {JoinSides}
 */
export type JSONJoin = [number, number];
